# Required to use the internal lead/subscriber endpoints.
ADMIN_KEY=replace-with-a-long-random-secret

# Optional: receive an email whenever an audit request is saved.
# These use Resend's REST API and require a verified sender address.
RESEND_API_KEY=
NOTIFICATION_FROM=ClickForge <leads@example.com>
NOTIFICATION_TO=hello@example.com
